/*
 * WeatherPage Messages
 *
 * This contains all the text for the WeatherPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.WeatherPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the WeatherPage container!"
  }
});
